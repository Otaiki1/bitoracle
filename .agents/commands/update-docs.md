# /stacks:update-docs

Update the local Stacks documentation index.

## Usage
- Run this periodically to ensure you have the latest information from `docs.stacks.co`.
- Uses the `update-docs-index.js` script to fetch and compress the latest docs list.
